define(function(require) {
	var assert = require('chai').assert;

	suite('...', function() {
		test('...', function() {
			// arrange

			// act

			// assert
			assert.equal("bla", "bla");
		});
	});
});